Running the project:

There are two available maven profiles: dev and demo

1. Add config that starts the project with spring-boot:run and specify the demo profile
2. If you want to see the in-memory database navigate to this link: http://localhost:8080/h2-console/
The user name is: sa
The password is: password